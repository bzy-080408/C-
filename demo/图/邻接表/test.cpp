

#include <bits/stdc++.h>
#include "main.h"

#define MAXN 100

using namespace std;

int main(){
    GraphEdge edge[MAXN];
    int head[MAXN];
    int GraphTimer
    int from,int to,int w,int m,int v = 0;
    cin >> m >> v;
    for(int i = 0;i < m;i++){
        cin >> from >> to >> w;
        AddEdge(from,to,w,edge,head,GraphTimer);
    }
    for(int i = 0;i < v;i++){
        while(edge[i].next != 0){
            
        } 

    }
}